---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_node_certificate Data Source - pve"
subcategory: ""
description: |-
  Reads the certificates deployed on a node (GET /nodes/{node}/certificates/info). Rows cover the cluster CA material, the default web certificate, and any custom or ACME chain.
---

# pve_node_certificate (Data Source)

Reads the certificates deployed on a node (`GET /nodes/{node}/certificates/info`). Rows cover the cluster CA material, the default web certificate, and any custom or ACME chain.

## Example Usage

```terraform
# Copyright IBM Corp. 2021, 2026
# SPDX-License-Identifier: MPL-2.0

data "pve_node_certificate" "pve1" {
  node = "pve1"
}

output "pve1_web_certificate_fingerprint" {
  value = [for cert in data.pve_node_certificate.pve1.certificates : cert.fingerprint if cert.filename == "pveproxy-ssl.pem"]
}

output "pve1_web_certificate_not_after" {
  value = [for cert in data.pve_node_certificate.pve1.certificates : cert.not_after if cert.filename == "pveproxy-ssl.pem"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `node` (String) The cluster node name.

### Read-Only

- `certificates` (Attributes List) Certificates installed on the node, one entry per info row. (see [below for nested schema](#nestedatt--certificates))
- `id` (String) Identifier of the data source; equals the `node` name.

<a id="nestedatt--certificates"></a>
### Nested Schema for `certificates`

Read-Only:

- `filename` (String) Base name of the installed certificate file (e.g. `pveproxy-ssl.pem`).
- `fingerprint` (String) Certificate SHA 256 fingerprint.
- `issuer` (String) Certificate issuer name.
- `not_after` (Number) Certificate notAfter timestamp (UNIX epoch).
- `not_before` (Number) Certificate notBefore timestamp (UNIX epoch).
- `pem` (String) Certificate in PEM format.
- `public_key_bits` (Number) Certificate public key size in bits.
- `public_key_type` (String) Certificate public key algorithm (e.g. `rsa`).
- `san` (List of String) SubjectAlternativeName entries of the certificate.
- `subject` (String) Certificate subject name.
