---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_cluster_status Data Source - pve"
subcategory: ""
description: |-
  Cluster status information from GET /cluster/status, plus the corosync totem settings (GET /cluster/config/totem) and QDevice status (GET /cluster/config/qdevice).
---

# pve_cluster_status (Data Source)

Cluster status information from `GET /cluster/status`, plus the corosync totem settings (`GET /cluster/config/totem`) and QDevice status (`GET /cluster/config/qdevice`).

## Example Usage

```terraform
# Copyright (c) HashiCorp, Inc.

# Cluster membership, quorum, corosync totem settings, and QDevice status.

data "pve_cluster_status" "cluster" {}

output "cluster_name" {
  value = data.pve_cluster_status.cluster.name
}

output "quorate" {
  value = data.pve_cluster_status.cluster.quorate
}

output "online_nodes" {
  value = [for n in data.pve_cluster_status.cluster.nodes : n.name if n.online]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `id` (String) Static identifier for the cluster status data source.
- `name` (String) Name of the cluster, from the summary entry (null on a standalone node).
- `nodes` (Attributes List) Per-node cluster membership entries. (see [below for nested schema](#nestedatt--nodes))
- `nodes_count` (Number) Node count of the cluster, including offline nodes (null on a standalone node).
- `qdevice` (Map of String) QDevice status as a name/value map, rendered the same way as `totem`.
- `quorate` (Boolean) Whether a majority of nodes is online so the cluster can make decisions (null on a standalone node).
- `totem` (Map of String) Corosync totem protocol settings as a name/value map. PVE types this object loosely, so non-string values are rendered as their literal text.
- `version` (Number) Current version of the corosync configuration file (null on a standalone node).

<a id="nestedatt--nodes"></a>
### Nested Schema for `nodes`

Read-Only:

- `id` (String) Entry ID (e.g. `node/pve1`).
- `ip` (String) IP of the resolved node name.
- `level` (String) Proxmox VE subscription level of the node.
- `local` (Boolean) Whether this is the responding node.
- `name` (String) Node name.
- `nodeid` (Number) Node ID from the corosync configuration.
- `online` (Boolean) Whether the node is online.
