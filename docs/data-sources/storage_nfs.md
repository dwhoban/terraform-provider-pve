---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_storage_nfs Data Source - pve"
subcategory: ""
description: |-
  Reads an NFS storage definition from GET /storage/{storage}. Errors when the storage's upstream type is not nfs.
---

# pve_storage_nfs (Data Source)

Reads an NFS storage definition from `GET /storage/{storage}`. Errors when the storage's upstream type is not `nfs`.

## Example Usage

```terraform
data "pve_storage_nfs" "media" {
  storage = "media"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `storage` (String) The storage identifier to look up (PVE `pve-storage-id` format).

### Read-Only

- `content` (Set of String) Allowed content types as a set (PVE `pve-storage-content-list`). NFS storages typically serve `images`, `iso`, `vztmpl`, and `backup`.
- `digest` (String) The read-only storage configuration revision (PVE `digest`).
- `disable` (Boolean) Whether the storage is disabled.
- `export` (String) NFS export path on the server (PVE `pve-storage-path` format).
- `max_protected_backups` (Number) Maximal number of protected backups per guest (PVE `max-protected-backups`). `-1` means unlimited.
- `nodes` (String) Comma-separated list of cluster node names the storage configuration applies to (PVE `pve-node-list`).
- `options` (String) NFS/CIFS mount options (see `man nfs`) (PVE `pve-storage-options`).
- `prune_backups` (String) The retention options in PVE `prune-backups` format, for example `keep-last=3,keep-daily=7`.
- `server` (String) Server IP or DNS name hosting the NFS export.
- `shared` (Boolean) Whether the storage is marked as shared with the same contents on all nodes.
