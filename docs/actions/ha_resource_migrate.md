---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_ha_resource_migrate Action - pve"
subcategory: ""
description: |-
  Requests online migration of an HA resource to another node (POST /cluster/ha/resources/{sid}/migrate). The request is queued with the HA manager, which performs the actual migration; this action returns once the request is accepted, because upstream reports no task UPID to wait on. Requires the Sys.Console privilege on /.
---

# pve_ha_resource_migrate (Action)

Requests online migration of an HA resource to another node (`POST /cluster/ha/resources/{sid}/migrate`). The request is queued with the HA manager, which performs the actual migration; this action returns once the request is accepted, because upstream reports no task UPID to wait on. Requires the `Sys.Console` privilege on `/`.

## Example Usage

```terraform
# Copyright IBM Corp. 2021, 2026
# SPDX-License-Identifier: MPL-2.0

# Request an online (live) migration of the HA-managed VM 100 to node
# pve2. The HA manager performs the migration after the request is
# accepted.
action "pve_ha_resource_migrate" "migrate_vm100" {
  config {
    sid  = "vm:100"
    node = "pve2"
  }
}
```

<!-- action schema generated by tfplugindocs -->
## Schema

### Required

- `node` (String) Target node to migrate the HA resource to.
- `sid` (String) HA resource ID, `<type>:<name>` (e.g. `vm:100` / `ct:100`); the bare VM or CT ID (e.g. `100`) is also accepted.
