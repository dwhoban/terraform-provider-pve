---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_ha_resource_relocate Action - pve"
subcategory: ""
description: |-
  Requests relocation of an HA resource to another node (POST /cluster/ha/resources/{sid}/relocate): unlike a migrate, the service is stopped on the old node and restarted on the target, causing downtime. The request is queued with the HA manager; this action returns once the request is accepted, because upstream reports no task UPID to wait on. Requires the Sys.Console privilege on /.
---

# pve_ha_resource_relocate (Action)

Requests relocation of an HA resource to another node (`POST /cluster/ha/resources/{sid}/relocate`): unlike a migrate, the service is stopped on the old node and restarted on the target, causing downtime. The request is queued with the HA manager; this action returns once the request is accepted, because upstream reports no task UPID to wait on. Requires the `Sys.Console` privilege on `/`.

## Example Usage

```terraform
# Copyright IBM Corp. 2021, 2026
# SPDX-License-Identifier: MPL-2.0

# Request relocation of the HA-managed container 101 to node pve3. Unlike
# a migrate, this stops the service on the old node and restarts it on the
# target, causing downtime.
action "pve_ha_resource_relocate" "relocate_ct101" {
  config {
    sid  = "ct:101"
    node = "pve3"
  }
}
```

<!-- action schema generated by tfplugindocs -->
## Schema

### Required

- `node` (String) Target node to relocate the HA resource to.
- `sid` (String) HA resource ID, `<type>:<name>` (e.g. `vm:100` / `ct:100`); the bare VM or CT ID (e.g. `100`) is also accepted.
