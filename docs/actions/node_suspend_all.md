---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_node_suspend_all Action - pve"
subcategory: ""
description: |-
  Disruptive: suspends all VMs on this node via POST /nodes/{node}/suspendall and waits for the worker task to finish. Suspended VMs keep their memory allocated on the node.
---

# pve_node_suspend_all (Action)

**Disruptive:** suspends all VMs on this node via `POST /nodes/{node}/suspendall` and waits for the worker task to finish. Suspended VMs keep their memory allocated on the node.

## Example Usage

```terraform
# Copyright (c) HashiCorp, Inc.

# Suspends all VMs on node pve1; their memory stays allocated. Invoke with:
#   apply it from a resource lifecycle block: actions = [action.pve_node_suspend_all.suspend_pve1]
action "pve_node_suspend_all" "suspend_pve1" {
  config {
    node = "pve1"
  }
}
```

<!-- action schema generated by tfplugindocs -->
## Schema

### Required

- `node` (String) The cluster node whose guests are suspended.

### Optional

- `max_workers` (Number) Maximum number of parallel jobs. Must be between 1 and 64. When unset, PVE uses `max_workers` from datacenter.cfg, falling back to the available CPU threads clamped to a maximum of 8.
- `vms` (String) Only consider guests with these IDs (comma-separated VMID list), e.g. `100,101`.
