---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_node_start_all Action - pve"
subcategory: ""
description: |-
  Starts all VMs and containers located on this node via POST /nodes/{node}/startall (by default only those with onboot=1) and waits for the worker task to finish.
---

# pve_node_start_all (Action)

Starts all VMs and containers located on this node via `POST /nodes/{node}/startall` (by default only those with `onboot=1`) and waits for the worker task to finish.

## Example Usage

```terraform
# Copyright IBM Corp. 2021, 2026
# SPDX-License-Identifier: MPL-2.0

# Starts every onboot guest on node pve1. Invoke with:
#   terraform apply -invoke pve_node_start_all.start_pve1
invoke "pve_node_start_all" "start_pve1" {
  config {
    node = "pve1"
  }
}
```

<!-- action schema generated by tfplugindocs -->
## Schema

### Required

- `node` (String) The cluster node whose guests are started.

### Optional

- `force` (Boolean) Issue the start command even if guests have `onboot` not set or set to off. Defaults to off.
- `max_workers` (Number) Maximum number of tasks running concurrently. Must be between 1 and 64. When unset, PVE uses `max_workers` from datacenter.cfg, falling back to the available CPU threads clamped to a maximum of 8.
- `vms` (String) Only consider guests from this comma-separated list of VMIDs, e.g. `100,101`.
