---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_guest_bulk_suspend Action - pve"
subcategory: ""
description: |-
  Disruptive: bulk suspends all guests on the cluster via POST /cluster/bulk-action/guest/suspend and waits for the worker task to finish. With to_disk the guests are suspended to disk and are resumed on their next start.
---

# pve_guest_bulk_suspend (Action)

**Disruptive:** bulk suspends all guests on the cluster via `POST /cluster/bulk-action/guest/suspend` and waits for the worker task to finish. With `to_disk` the guests are suspended to disk and are resumed on their next start.

## Example Usage

```terraform
# Copyright IBM Corp. 2021, 2026
# SPDX-License-Identifier: MPL-2.0

# Suspends the given guests cluster-wide to disk. Invoke with:
#   apply it from a resource lifecycle block: actions = [action.pve_guest_bulk_suspend.suspend_vm100]
action "pve_guest_bulk_suspend" "suspend_vm100" {
  config {
    vms     = [100]
    to_disk = true
  }
}
```

<!-- action schema generated by tfplugindocs -->
## Schema

### Optional

- `max_workers` (Number) Maximum number of tasks running concurrently. Must be between 1 and 64. Defaults to 4 on the API.
- `state_storage` (String) The storage for the VM state; requires `to_disk`.
- `to_disk` (Boolean) If set, suspends the guests to disk. They are resumed on the next start. Defaults to false on the API.
- `vms` (Set of Number) Only consider guests from this set of VMIDs, e.g. `[100, 101]`.
