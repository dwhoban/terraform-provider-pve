---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_storage_pbs Resource - pve"
subcategory: ""
description: |-
  Manages a Proxmox Backup Server storage definition (/storage, type pbs). Changing storage forces recreation. Deletion is synchronous per the pin (no task is spawned).
---

# pve_storage_pbs (Resource)

Manages a Proxmox Backup Server storage definition (`/storage`, type `pbs`). Changing `storage` forces recreation. Deletion is synchronous per the pin (no task is spawned).

## Example Usage

```terraform
variable "pbs_password" {
  type      = string
  sensitive = true
  default   = "replace-with-pbs-password"
}

# Proxmox Backup Server datastore for VM backups.
resource "pve_storage_pbs" "pbs1" {
  storage       = "pbs1"
  server        = "192.168.1.10"
  datastore     = "store1"
  username      = "backup@pbs"
  password      = var.pbs_password
  fingerprint   = "AA:BB:CC:DD:EE:FF:00:11:22:33:44:55:66:77:88:99:AA:BB:CC:DD:EE:FF:00:11:22:33:44:55:66:77:88:99"
  content       = ["backup"]
  prune_backups = "keep-last=3,keep-daily=7"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `datastore` (String) The Proxmox Backup Server datastore name.
- `server` (String) The Proxmox Backup Server address (name or IP).
- `storage` (String) The storage identifier (PVE `pve-storage-id` format). Changing this value forces recreation.
- `username` (String) The user name or API token ID used to authenticate against the datastore (for example `root@pam` or `backup@pbs!token`).

### Optional

- `content` (Set of String) Allowed content types as a set (PVE `pve-storage-content-list`). For `pbs` storages the expected value is `backup`. PVE validates the set upstream.
- `disable` (Boolean) Flag to disable the storage.
- `fingerprint` (String) The server certificate SHA 256 fingerprint.
- `master_pubkey` (String) Base64-encoded, PEM-formatted public RSA master key (PVE `master-pubkey`). Used to encrypt a copy of the encryption key added to each encrypted backup.
- `max_protected_backups` (Number) Maximal number of protected backups per guest (PVE `max-protected-backups`). Use `-1` for unlimited. Must be -1 or greater.
- `namespace` (String) The Proxmox Backup Server namespace.
- `nodes` (String) Comma-separated list of cluster node names the storage configuration applies to (PVE `pve-node-list`).
- `password` (String, Sensitive) The user password or API token secret used to authenticate against the datastore.
- `port` (Number) Optional port used to connect to the server instead of the default (`8007`). Must be between 1 and 65535 inclusive.
- `prune_backups` (String) The retention options in PVE `prune-backups` format, for example `keep-last=3,keep-daily=7`.
- `skip_cert_verification` (Boolean) Disable TLS certificate verification (PVE `skip-cert-verification`); only enable on fully trusted networks.

### Read-Only

- `digest` (String) The read-only storage configuration revision (PVE `digest`).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import an existing storage definition by its storage identifier:
terraform import pve_storage_pbs.pbs1 pbs1
```
