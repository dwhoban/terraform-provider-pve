---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_hardware_mapping_usb Resource - pve"
subcategory: ""
description: |-
  Manages a logical USB hardware mapping (/cluster/mapping/usb), declaring one entry per cluster node that carries the device. Updates replace the full entry list per the pin's PUT semantics.
---

# pve_hardware_mapping_usb (Resource)

Manages a logical USB hardware mapping (`/cluster/mapping/usb`), declaring one entry per cluster node that carries the device. Updates replace the full entry list per the pin's PUT semantics.

## Example Usage

```terraform
resource "pve_hardware_mapping_usb" "ups" {
  id          = "ups"
  description = "UPS serial link"

  map = [{
    node = "pve1"
    id   = "8087:0a2a"
    path = "1-2"
  }]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) The ID of the logical USB mapping (PVE `pve-configid` format). Changing this value forces recreation.
- `map` (Attributes List) Per-node device entries; one entry per node that carries the hardware. (see [below for nested schema](#nestedatt--map))

### Optional

- `description` (String) Description of the logical USB device (up to 4096 characters).

<a id="nestedatt--map"></a>
### Nested Schema for `map`

Required:

- `node` (String) The cluster node name.

Optional:

- `description` (String) Description of the node-specific device (up to 4096 characters).
- `id` (String) Expected vendor and device ID in `vendor:device` form (e.g. `8087:0a2a`); with a `path` given it is only used for detecting hardware changes.
- `path` (String) USB port path of the device in `<bus>-<port>` form (e.g. `1-2` or `1-2.3`).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import an existing USB mapping by its identifier:
terraform import pve_hardware_mapping_usb.ups ups
```
