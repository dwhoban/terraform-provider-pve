---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_container_snapshot Resource - pve"
subcategory: ""
description: |-
  Creates and destroys one snapshot of an LXC container (POST/DELETE /nodes/{node}/lxc/{vmid}/snapshot). The snapshot name is immutable: changing name, node, or vmid forces replacement. Only the description updates in place (PUT .../snapshot/{snapname}/config). Requires the VM.Snapshot privilege on /vms/{vmid}.
---

# pve_container_snapshot (Resource)

Creates and destroys one snapshot of an LXC container (`POST/DELETE /nodes/{node}/lxc/{vmid}/snapshot`). The snapshot name is immutable: changing `name`, `node`, or `vmid` forces replacement. Only the `description` updates in place (`PUT .../snapshot/{snapname}/config`). Requires the `VM.Snapshot` privilege on `/vms/{vmid}`.

## Example Usage

```terraform
# Copyright (c) HashiCorp, Inc.

# Snapshot of container 100 taken before each upgrade. The snapshot name is
# immutable: only the description updates in place.
resource "pve_container_snapshot" "pre_upgrade" {
  node        = "pve1"
  vmid        = 100
  name        = "pre-upgrade"
  description = "Taken before upgrading the container"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the snapshot (upstream `pve-configid`: at most 40 characters). The reserved name `current` refers to the live state and cannot be used. Changing this value forces recreation.
- `node` (String) The cluster node the container runs on. Changing this value forces recreation.
- `vmid` (Number) The (unique) ID of the container. Must be between 100 and 999999999. Changing this value forces recreation.

### Optional

- `description` (String) A textual description or comment stored with the snapshot. Updating it rewrites the snapshot metadata only.

### Read-Only

- `parent` (String) Name of the parent snapshot in the snapshot tree, when one exists.
- `snaptime` (Number) Unix epoch timestamp of when the snapshot was taken.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Copyright (c) HashiCorp, Inc.
# Import an existing snapshot by its node, vmid, and name:
terraform import pve_container_snapshot.pre_upgrade pve1/100/pre-upgrade
```
