---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_storage_iscsidirect Resource - pve"
subcategory: ""
description: |-
  Manages an iSCSI direct-attached storage definition (/storage, type iscsidirect). Volumes are shared block devices; no snapshot or cloning is possible. Changing storage, portal, or target forces recreation. Deletion is synchronous per the pin (no task is spawned).
---

# pve_storage_iscsidirect (Resource)

Manages an iSCSI direct-attached storage definition (`/storage`, type `iscsidirect`). Volumes are shared block devices; no snapshot or cloning is possible. Changing `storage`, `portal`, or `target` forces recreation. Deletion is synchronous per the pin (no task is spawned).

## Example Usage

```terraform
# Copyright (c) HashiCorp, Inc.

# Manage an iSCSI direct-attached storage definition. Volumes are shared
# block devices; no snapshot or cloning is possible.
resource "pve_storage_iscsidirect" "fast" {
  storage = "fast"
  portal  = "192.168.1.10:3260"
  target  = "iqn.2000-01.com.example:fast.target0"

  nowritecache = true
  content      = ["images"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `portal` (String) iSCSI portal (IP or DNS name with optional port), for example `192.168.1.10:3260` (PVE `pve-storage-portal-dns`). The pin's update verb does not accept this field, so changing it forces recreation.
- `storage` (String) The storage identifier (PVE `pve-storage-id` format). Changing this value forces recreation.
- `target` (String) iSCSI target, for example `iqn.2000-01.com.example:fast.target0`. The pin's update verb does not accept this field, so changing it forces recreation.

### Optional

- `content` (Set of String) Allowed content types as a set (PVE `pve-storage-content-list`). iSCSI direct storages only support `images`. PVE validates the set upstream.
- `disable` (Boolean) Flag to disable the storage.
- `max_protected_backups` (Number) Maximal number of protected backups per guest (PVE `max-protected-backups`). Use `-1` for unlimited. Must be -1 or greater.
- `nodes` (String) Comma-separated list of cluster node names the storage configuration applies to (PVE `pve-node-list`).
- `nowritecache` (Boolean) Disable write caching on the target (PVE `nowritecache`).
- `prune_backups` (String) The retention options in PVE `prune-backups` format, for example `keep-last=3,keep-daily=7`.
- `shared` (Boolean) Marks the storage as a single storage with the same contents on all nodes (or all nodes listed in `nodes`). It does not make a local storage's contents accessible to other nodes.

### Read-Only

- `digest` (String) The read-only storage configuration revision (PVE `digest`).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/bin/sh
# Copyright (c) HashiCorp, Inc.

# Import a PVE storage definition by its storage ID.
terraform import pve_storage_iscsidirect.fast "fast"
```
