---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_storage_cephfs Resource - pve"
subcategory: ""
description: |-
  Manages a CephFS storage definition (/storage, type cephfs). Changing storage forces recreation. Deletion is synchronous per the pin (no task is spawned).
---

# pve_storage_cephfs (Resource)

Manages a CephFS storage definition (`/storage`, type `cephfs`). Changing `storage` forces recreation. Deletion is synchronous per the pin (no task is spawned).

## Example Usage

```terraform
variable "ceph_keyring" {
  type      = string
  sensitive = true
  default   = "replace-with-ceph-keyring-contents"
}

# External CephFS cluster mounted as VM image/container storage.
resource "pve_storage_cephfs" "cephfs1" {
  storage  = "cephfs1"
  monhost  = "192.168.1.10:6789"
  username = "admin"
  keyring  = var.ceph_keyring
  fs_name  = "cephfs"
  subdir   = "/pve"
  content  = ["images", "rootdir"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `storage` (String) The storage identifier (PVE `pve-storage-id` format). Changing this value forces recreation.

### Optional

- `content` (Set of String) Allowed content types as a set (PVE `pve-storage-content-list`), for example `images` or `rootdir`. PVE validates the set upstream.
- `disable` (Boolean) Flag to disable the storage.
- `fs_name` (String) The Ceph filesystem name (PVE `fs-name`).
- `fuse` (Boolean) Mount CephFS through FUSE instead of the kernel client.
- `keyring` (String, Sensitive) Client keyring contents for external clusters.
- `monhost` (String) Comma-separated list of monitor addresses (IPs or DNS names, optionally with ports) of the Ceph cluster (PVE `pve-storage-portal-dns-list`).
- `nodes` (String) Comma-separated list of cluster node names the storage configuration applies to (PVE `pve-node-list`).
- `path` (String) File system path where the CephFS is mounted (PVE `pve-storage-path`).
- `subdir` (String) Subdirectory to mount (PVE `pve-storage-path`).
- `username` (String) The Ceph user name used to authenticate against the cluster.

### Read-Only

- `digest` (String) The read-only storage configuration revision (PVE `digest`).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import an existing storage definition by its storage identifier:
terraform import pve_storage_cephfs.cephfs1 cephfs1
```
