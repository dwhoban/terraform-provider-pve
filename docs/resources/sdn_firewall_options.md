---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_sdn_firewall_options Resource - pve"
subcategory: ""
description: |-
  Manages the firewall options singleton of one SDN vnet (GET/PUT /cluster/sdn/vnets/{vnet}/firewall/options). Every listed attribute is managed: removing an attribute from configuration clears the option via the delete parameter. The pin defines no delete verb for this scope, so destroy only forgets the state. Requires SDN.Allocate on /sdn/zones/<zone>/<vnet>.
---

# pve_sdn_firewall_options (Resource)

Manages the firewall options singleton of one SDN vnet (`GET/PUT /cluster/sdn/vnets/{vnet}/firewall/options`). Every listed attribute is managed: removing an attribute from configuration clears the option via the `delete` parameter. The pin defines no delete verb for this scope, so destroy only forgets the state. Requires `SDN.Allocate` on `/sdn/zones/<zone>/<vnet>`.

## Example Usage

```terraform
# Firewall options of the SDN vnet1. One instance per vnet manages the
# listed options; removing an attribute clears it via the `delete`
# parameter. Destroy only forgets the state.
resource "pve_sdn_firewall_options" "vnet1" {
  vnet   = "vnet1"
  enable = true

  policy_forward    = "DROP"
  log_level_forward = "debug"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `vnet` (String) The SDN vnet object identifier. Changing this value forces recreation.

### Optional

- `enable` (Boolean) Enable/disable firewall rules.
- `log_level_forward` (String) Log level for forwarded traffic. Must be one of: `emerg`, `alert`, `crit`, `err`, `warning`, `notice`, `info`, `debug`, `nolog`.
- `policy_forward` (String) Forward policy. Must be one of: `ACCEPT`, `DROP`.

### Read-Only

- `id` (String) Identifier of the vnet firewall options singleton; equals the `vnet` name.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import ID is the vnet identifier.
terraform import pve_sdn_firewall_options.vnet1 vnet1
```
