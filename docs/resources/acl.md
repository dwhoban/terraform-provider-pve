---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_acl Resource - pve"
subcategory: ""
description: |-
  Manages a single access control list entry on the Proxmox VE cluster (GET/PUT /access/acl). Changing path, role, or type replaces the entry; deletion issues a PUT with the delete flag because the API defines no DELETE verb on this path.
---

# pve_acl (Resource)

Manages a single access control list entry on the Proxmox VE cluster (`GET/PUT /access/acl`). Changing `path`, `role`, or `type` replaces the entry; deletion issues a PUT with the `delete` flag because the API defines no DELETE verb on this path.

## Example Usage

```terraform
// Copyright IBM Corp. 2021, 2026
// SPDX-License-Identifier: MPL-2.0

resource "pve_acl" "example" {
  path      = "/vms/100"
  role      = "PVEVMUser"
  type      = "user"
  user_id   = "ops@pam"
  propagate = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) Access control path the entry applies to, e.g. `/`, `/vms/100`, or `/storage/local`.
- `role` (String) Role name granted on `path`, e.g. `Administrator` or `PVEVMUser`.
- `type` (String) Kind of subject the entry grants to. Must be one of: `user`, `group`, `token`.

### Optional

- `group_id` (String) Group ID when `type` is `group`. Exactly one of `user_id`, `group_id`, or `token_id` must be set, matching `type`.
- `propagate` (Boolean) Allow the permission to propagate (inherit) to child paths. Defaults to `true` (the PVE server default).
- `token_id` (String) Full API token ID (`user@realm!tokenname`) when `type` is `token`. Exactly one of `user_id`, `group_id`, or `token_id` must be set, matching `type`.
- `user_id` (String) User ID (`user@realm`) when `type` is `user`. Exactly one of `user_id`, `group_id`, or `token_id` must be set, matching `type`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Copyright IBM Corp. 2021, 2026
# SPDX-License-Identifier: MPL-2.0

terraform import pve_acl.example '/vms/100|PVEVMUser|user|ops@pam'
```
