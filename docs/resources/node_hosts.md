---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_node_hosts Resource - pve"
subcategory: ""
description: |-
  Manages the /etc/hosts file of one node (GET/POST /nodes/{node}/hosts). Every write replaces the whole file, so the entries list is authoritative and other (out-of-band) lines are lost on apply. Removing this resource from configuration only forgets the state — the file on the node is left untouched because PVE offers no way to delete /etc/hosts content.
---

# pve_node_hosts (Resource)

Manages the `/etc/hosts` file of one node (`GET/POST /nodes/{node}/hosts`). Every write replaces the whole file, so the `entries` list is authoritative and other (out-of-band) lines are lost on apply. Removing this resource from configuration only forgets the state — the file on the node is left untouched because PVE offers no way to delete `/etc/hosts` content.

## Example Usage

```terraform
# Copyright IBM Corp. 2021, 2026
# SPDX-License-Identifier: MPL-2.0

resource "pve_node_hosts" "pve1" {
  node = "pve1"

  entries = [
    {
      address   = "127.0.0.1"
      hostnames = ["localhost.localdomain", "localhost"]
    },
    {
      address   = "192.168.1.10"
      hostnames = ["pve1.local", "pve1"]
    },
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `entries` (Attributes List) Ordered entries of the hosts file; each renders as `<address> <hostname>...` on its own line. The list order is the file order. (see [below for nested schema](#nestedatt--entries))
- `node` (String) The cluster node name whose `/etc/hosts` is managed.

### Read-Only

- `digest` (String) Digest of the current file as reported by the node; used for concurrent-modification detection on write.
- `id` (String) Identifier of the resource; equals `node`.

<a id="nestedatt--entries"></a>
### Nested Schema for `entries`

Required:

- `address` (String) IP address of the entry.
- `hostnames` (List of String) Hostnames of the entry; the first is the canonical name.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Copyright IBM Corp. 2021, 2026
# SPDX-License-Identifier: MPL-2.0

# The import ID is the node name.
terraform import pve_node_hosts.pve1 pve1
```
