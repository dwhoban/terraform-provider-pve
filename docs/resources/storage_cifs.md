---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pve_storage_cifs Resource - pve"
subcategory: ""
description: |-
  Manages a CIFS storage definition (/storage, type cifs). Changing storage or share forces recreation. Deletion is synchronous per the pin (no task is spawned).
---

# pve_storage_cifs (Resource)

Manages a CIFS storage definition (`/storage`, type `cifs`). Changing `storage` or `share` forces recreation. Deletion is synchronous per the pin (no task is spawned).

## Example Usage

```terraform
# Copyright (c) HashiCorp, Inc.

# Manage a CIFS/SMB share as a PVE storage definition.
resource "pve_storage_cifs" "archive" {
  storage    = "archive"
  server     = "192.168.1.10"
  share      = "archive"
  username   = "backup"
  domain     = "CORP"
  smbversion = "3.11"

  content = ["backup"]

  # PVE never returns the password on reads; it is sent on every apply
  # that carries it.
  password = var.archive_password
}

variable "archive_password" {
  type      = string
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `server` (String) Server IP or DNS name hosting the CIFS share.
- `share` (String) CIFS share name. The pin's update verb does not accept this field, so changing it forces recreation.
- `storage` (String) The storage identifier (PVE `pve-storage-id` format). Changing this value forces recreation.

### Optional

- `content` (Set of String) Allowed content types as a set (PVE `pve-storage-content-list`). CIFS storages typically serve `backup`, `iso`, `vztmpl`, and `images`. PVE validates the set upstream.
- `disable` (Boolean) Flag to disable the storage.
- `domain` (String) CIFS domain (PVE `domain`).
- `max_protected_backups` (Number) Maximal number of protected backups per guest (PVE `max-protected-backups`). Use `-1` for unlimited. Must be -1 or greater.
- `nodes` (String) Comma-separated list of cluster node names the storage configuration applies to (PVE `pve-node-list`).
- `options` (String) NFS/CIFS mount options (see `man mount.cifs`) (PVE `pve-storage-options`).
- `password` (String, Sensitive) Password for accessing the share. PVE never returns the password on reads.
- `prune_backups` (String) The retention options in PVE `prune-backups` format, for example `keep-last=3,keep-daily=7`.
- `shared` (Boolean) Marks the storage as a single storage with the same contents on all nodes (or all nodes listed in `nodes`). It does not make a local storage's contents accessible to other nodes.
- `smbversion` (String) SMB protocol version (PVE `smbversion`). `default` negotiates the highest SMB2+ version supported by both the client and server. Must be one of: `default`, `2.0`, `2.1`, `3`, `3.0`, `3.11`.
- `username` (String) User name used to access the share.

### Read-Only

- `digest` (String) The read-only storage configuration revision (PVE `digest`).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/bin/sh
# Copyright (c) HashiCorp, Inc.

# Import a PVE storage definition by its storage ID.
terraform import pve_storage_cifs.archive "archive"
```
